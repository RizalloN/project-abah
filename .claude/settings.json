{
  "permissions": {
    "allow": [
      "Bash(mkdir -p public/vendor/chartjs)",
      "Bash(wget -q https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js -O chart.min.js)",
      "Bash(php -r ' *)",
      "Bash(git commit -m 'Fix: Use local Chart.js instead of CDN + improve canvas sizing *)"
    ]
  }
}
